#include<iostream>
using namespace std;

vector<int> runningSum(vector<int>& nums) {
        
        int i;
        for(i=1;i<nums.size();i++){
            nums[i]=nums[i]+nums[i-1];
            
            
        }

        return nums;
}
